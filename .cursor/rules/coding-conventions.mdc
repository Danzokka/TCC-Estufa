---
globs: *.ts,*.tsx,*.js,*.jsx,*.py,*.cpp,*.h
description: Convenções de código para o projeto TCC-Estufa
---

# Convenções de Código - TCC-Estufa

## 📝 Convenções Gerais

### Nomenclatura

- **camelCase** para variáveis e funções
- **PascalCase** para classes e componentes
- **SCREAMING_SNAKE_CASE** para constantes
- **kebab-case** para arquivos e pastas

### Comentários

- Use **português** para comentários explicativos
- Documente funções complexas
- Mantenha comentários atualizados

## 🎨 Frontend (Next.js/React)

### Componentes

```typescript
// ✅ Bom
export function SensorCard({ sensor, value }: SensorCardProps) {
  return (
    <div className="rounded-lg border p-4">
      <h3 className="font-semibold">{sensor.name}</h3>
      <p className="text-2xl font-bold">{value}</p>
    </div>
  );
}

// ❌ Evitar
export function sensorCard(props) {
  return <div>{props.sensor.name}: {props.value}</div>;
}
```

### Hooks

- Use hooks customizados para lógica reutilizável
- Prefira `useQuery` para dados da API
- Use `useState` para estado local simples

### Styling

- **Tailwind CSS** para estilização
- Use classes utilitárias
- Mantenha consistência visual

## 🔧 Backend (NestJS)

### Estrutura de Módulos

```typescript
// ✅ Bom
@Module({
  imports: [PrismaModule, ConfigModule],
  controllers: [SensorController],
  providers: [SensorService],
  exports: [SensorService],
})
export class SensorModule {}
```

### DTOs e Validação

```typescript
// ✅ Bom
export class CreateSensorDto {
  @IsString()
  @IsNotEmpty()
  name: string;

  @IsNumber()
  @Min(0)
  @Max(100)
  value: number;
}
```

### Services

- Use **dependency injection**
- Mantenha métodos pequenos e focados
- Implemente tratamento de erros robusto

## 🤖 IA (Python)

### Estrutura de Funções

```python
# ✅ Bom
def process_sensor_data(data: pd.DataFrame) -> Dict[str, float]:
    """
    Processa dados dos sensores e retorna estatísticas.

    Args:
        data: DataFrame com dados dos sensores

    Returns:
        Dict com estatísticas processadas
    """
    # Implementação aqui
    pass
```

### Imports

- Organize imports por categoria
- Use imports absolutos quando possível
- Mantenha imports limpos

## 🔌 ESP32 (C++)

### Estrutura de Classes

```cpp
// ✅ Bom
class SensorManager {
private:
    int pin;
    float lastValue;

public:
    SensorManager(int sensorPin);
    float readValue();
    bool isConnected();
};
```

### Naming Conventions

- **snake_case** para variáveis e funções
- **PascalCase** para classes
- **UPPER_CASE** para constantes e defines

## 📊 Banco de Dados

### Schema Prisma

```prisma
// ✅ Bom
model Sensor {
  id        String   @id @default(uuid())
  name      String   @unique
  value     Float
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("sensors")
}
```

### Migrations

- Use nomes descritivos para migrations
- Mantenha migrations pequenas e focadas
- Sempre teste migrations em desenvolvimento

## 🧪 Testes

### Estrutura de Testes

```typescript
// ✅ Bom
describe("SensorService", () => {
  let service: SensorService;

  beforeEach(async () => {
    const module = await Test.createTestingModule({
      providers: [SensorService],
    }).compile();

    service = module.get<SensorService>(SensorService);
  });

  it("should create a sensor", async () => {
    const result = await service.create(mockSensorData);
    expect(result).toBeDefined();
  });
});
```

## 📝 Documentação

### README

- Mantenha README atualizado
- Inclua instruções de instalação
- Documente APIs principais

### Comentários de Código

- Use JSDoc para funções TypeScript
- Documente parâmetros e retornos
- Mantenha exemplos de uso quando necessário
